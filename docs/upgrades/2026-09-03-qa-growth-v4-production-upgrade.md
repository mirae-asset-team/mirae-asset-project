# QA Growth v4 — 8000 프로덕션 업그레이드 (2026-09-03)

브랜치 `agent/qa-growth-v4`를 프로덕션 `101.79.31.221:8000`에 반영한 기록.
기존(승격 직전 실행 중이던 이미지, 2026-09-01 빌드) 대비 무엇을 고치고 무엇을 올렸는지 정리한다.

## 한 줄 요약

같은 재무 grain인데 **답변 단위를 지정하면 표시 배율이 깨지던 결함군**과, **파생지표·문서형·투자권유 질의가 HyperCLOVA 도구선택으로 흘러 느리고 대부분 틀리던 라우팅 공백**을 닫았다. QA 하네스는 자기일관성 검사에서 **코퍼스 대조 정답 검증**으로 승격했다.

## 기존 대비 수정한 결함 (프로덕션 실측으로 확인)

| # | 결함 | 기존 8000 | 승격 후 8000 |
|---|---|---|---|
| 1 | 매출액 표시 배율 | `33,360억` (100배 축소) | `333조 6,059억 3,800만 원` |
| 2 | 부채비율(2계정 파생) | 도구선택 실패→보류(≈4s) | `29.94%` 결정론 계산(≈1s) |
| 3 | 단위 지정 "조" | `561조`(1만배) | `0.0561363조`(정답) |
| 4 | 단위 지정 "원" | `1,146.8억`(100배 축소) | `11,467,851,000,000원`(정답) |
| 5 | 달러 환산 요청 | 금액 10배 오표기 | 정답 금액 + 환산 불가 안내 |
| 6 | 거짓 전제 방향 | "증가한 이유는…"(전제 수용) | "증가 전제는 공시 수치와 일치하지 않습니다…감소" |
| 7 | 배당·연구개발 등 문서형 | 도구선택 오선택→0% 응답 | 검색 라우팅으로 근거 답변 |
| 8 | 투자권유("지금 사도 될까") | ≈9s 후 오류/보류 | 즉시 결정론 거절(≈0.2s) |

## 무엇을 바꿨나 (코드)

### 1. 라우팅 — HyperCLOVA 도구선택 우회 (`question_routing.py`, `calculator.py`, `hcx_function_calling.py`)
- **2계정 파생비율**(부채비율·영업이익률·순이익률·자기자본이익률)을 결정론 라우팅으로 보내고, 검증된 두 재무 fact에서 **실제로 계산**한다. `calculator`에 `percentage_ratio` 연산을 추가하고, 비교 실행기가 공유 근거집합 위에서 비율을 산출한다. (카탈로그에 공식은 있었으나 실행이 꺼져 있던 것을 켠 것)
- **문서형 질의**(회사 + 공시 주제, 재무계정 없음)를 `search_disclosures`로 직행.
- **매매 권유**를 즉시 결정론 거절(도구선택 왕복 제거).

근거: 2026-09-02 프로덕션 5,119건 실측에서 도구선택 경로가 708건을 가져가 평균 5.45s(결정론 1.87s)를 쓰고, `derived_metric` 276/276·`dividend` 66/66·`research_development` 38/38을 **0% 응답**으로 끝냈다. 결정론 확대가 속도·정확도를 동시에 고친다.

### 2. QA 하네스 — 자기일관성 → 정답 대조 (`scripts/`)
- `build_qa_ground_truth.py`: 과거 응답에서 재무 grain을 수확하고 **인용 셀을 불변 base 코퍼스에서 재조회해 확증**(978/978, 모순 0). 손실 표기 `(1,234)`·`△1,234`를 음수로 읽는다.
  - 2026-09-05 정정: 당시 `978/978`은 숫자 셀 재현 결과이며 계정·전체 기간·연결/별도 의미까지 독립 검증한 수치가 아니다. 현재 builder는 별도 read-only overlay의 validated fact와 base evidence를 함께 대조하며, 새 기준으로 재생성하기 전까지 과거 수치를 의미 정확도 지표로 사용하지 않는다.
- `qa_bank_v4.py`: 회사×템플릿 곱셈이 아닌 **38개 새 능력 축**(정답 확인/반증, 단위 지정, 회계 항등식, 거짓 전제, 종목코드·부분명·잡음 입력, 상대 시점, 근거 요구, 유니버스 집계, 간접 안전 우회).
- `run_mass_qa.py`: 검증기 4종 추가(fact→인용셀 출처 일치, 인용셀 코퍼스 재조회, **정답 대조**, 심어둔 거짓 주장 수용 탐지). 러너 백오프는 **쿼터 즉시거절(0.1~0.3s)에만** 걸도록 고쳐 처리량 2건/분→29건/분 회복.

## 업그레이드 (완벽화)

- 표시 배율 결함을 "단위 지정"이라는 새 각도로 전수 노출: 기존 뱅크엔 단위 지정 질문 자체가 없어 못 잡던 것을, 조/억/원/환산 4형으로 갈라 정답 대조로 잡는다.
- 정답 테이블을 코퍼스로 확증해, 앞으로 모든 재무 수치 답변을 **내부 일관성이 아니라 실제 값**으로 채점한다.

## 배포 방식 (안전·가역)

1. **격리 검증**: 우리 브랜치를 `8002` 별도 컨테이너로 띄워(팀 8000/8001 무손상, 우리 IP만 ACG 허용) 6개 결함을 실서버에서 확인.
2. **승격**: 검증된 그 이미지를 `8000`에 프로덕션 rate limit(120/4/8)으로 올림. 기존 8000 컨테이너는 **정지만(삭제 안 함)**, 이미지는 `app-disclosure-agent:rollback-pre-qagrowth-20260903`로 태깅 → 즉시 롤백 가능.
3. **검증**: 외부에서 9/9 통과(6개 결함 + 일반조회·비교·주입방어 무회귀).

**롤백**: `docker rm -f qa-growth-v4-prod-prod-agent-1 && docker start app-disclosure-agent-1`.

## 팀 소스와의 관계 (내용 대조로 확정)

승격 전 팀의 현재 서버 소스(`/srv/mirae/app`)를 우리 브랜치와 **내용(LF 정규화) 기준**으로 전수 대조했다. 결과:

- 팀 서버 소스의 **모든 파일이 우리 브랜치 히스토리의 조상**이다 — 즉 우리 브랜치는 팀 현재 소스를 **완전히 포함하는 상위집합**이다.
- 라우팅 케이스 비교: 팀이 가진 라우팅 reason 중 **우리가 없는 것은 0개**, 우리가 팀에 없는 것은 14개(파생비율·회계항등식·문서형·투자권유·단위 지정·비공개정보 등).
- 처음엔 CRLF(서버 파일이 CRLF) 때문에 53개 파일이 다르게 보였고 "팀이 독립적으로 리팩터했다"고 오판했으나, 내용 대조에서 착시로 확인됐다. 예: "팀이 `disclosure_tools`에서 KRW 파서를 제거"가 아니라 팀 옛 버전엔 원래 없었고 **우리가 추가**한 것이다(`_KRW_AMOUNT_TOKEN` 팀 0 / 우리 2).

따라서 대표님 지시("팀이 더 정교하면 팀 걸 수용")의 결과는 **수용할 것 없음** — 서빙 경로 전 파일에서 우리 브랜치가 앞선다. 이번 8000 승격은 우리 검증 빌드 단독이며, 팀 소스의 어떤 부분도 되돌리지 않는다(그들 소스가 우리의 부분집합이므로).

## 미세 항목 (수정 완료, 재배포됨)

- 소액 grain에 "조 단위" 요청 시 `0.0561363조`처럼 어색하던 렌더링을 수정했다. 요청 단위(조/억)가 값 규모보다 크면 자연 혼합 단위로 폴백한다(값은 그대로 정확): 세아베스틸지주 순이익 `조 단위` → `561억 3,630만 9,970원`. 값이 요청 단위 1개 이상이면 그 단위로 정확히 렌더링(삼성 매출 → `333.605938조 원`). 커밋 `f1cea3d`, 이미지 재빌드 후 8000 재배포·라이브 검증 완료.
